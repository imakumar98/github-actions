name: Deploy to Dev Server

on:
  push:
    branches:
    - develop

jobs:
  deploy:
    name: Deploying the app to Dev Server
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v1
    - name: Deployment started here
    - run: Deployment process running...
      
    - name: deployment process running 2...
      run: |
        ls -la
